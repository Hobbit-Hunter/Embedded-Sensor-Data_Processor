#include <stdio.h>
#include "adc.h"
#include "stats.h"
#include "io.h"

int main() {
    sensor_log();
    mean_voltage(voltage, count);
        printf("mean voltage: %lf", mean_voltage);

    return 0;



}